const grid=document.querySelector('#projectGrid'), filters=document.querySelector('#filters');
const categories=['All',...new Set(PROJECTS.map(p=>p.category))];
function drawFilters(active='All'){filters.innerHTML=categories.map(c=>`<button class="filter ${c===active?'active':''}" data-c="${c}">${c}</button>`).join('');filters.querySelectorAll('button').forEach(b=>b.onclick=()=>{drawFilters(b.dataset.c);drawProjects(b.dataset.c)})}
function drawProjects(cat='All'){const items=cat==='All'?PROJECTS:PROJECTS.filter(p=>p.category===cat);grid.innerHTML=items.map((p,i)=>`<article class="project"><div class="project-img ${p.image?'has-image':''}" ${p.image?`style="background-image:url('${p.image}')"`:''}><span>${String(i+1).padStart(2,'0')}</span></div><div class="project-body"><div class="project-meta"><small>${p.category}</small><span>${p.tech.join(' · ')}</span></div><h3>${p.title}</h3><p>${p.description}</p><div class="links">${p.demo&&p.demo!=='#'?`<a href="${p.demo}" target="_blank">Live ↗</a>`:''}${p.github&&p.github!=='#'?`<a href="${p.github}" target="_blank">GitHub ↗</a>`:''}</div></div></article>`).join('')}
drawFilters();drawProjects();document.querySelector('#year').textContent=new Date().getFullYear();
const menu=document.querySelector('.menu'),nav=document.querySelector('.nav nav');menu.onclick=()=>nav.classList.toggle('open');nav.querySelectorAll('a').forEach(a=>a.onclick=()=>nav.classList.remove('open'));
